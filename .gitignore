# nothing here is built, so there is very little to ignore
node_modules/
.DS_Store
Thumbs.db
*.log

# never commit a filled-in copy: the placeholders in apps-script/ are there
# on purpose. Keep real secrets in the Apps Script project only.
*.local.gs
secrets.*
